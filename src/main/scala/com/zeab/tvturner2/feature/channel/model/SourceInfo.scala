package com.zeab.tvturner2.feature.channel.model

case class SourceInfo(
                   id: String,
                   name: String,
                   `type`: String,
                   uri: String
                 )
